{
  "mcpServers": {
    "projectplaner": {
      "command": "pnpm",
      "args": ["--filter", "@projectplaner/mcp", "start"],
      "cwd": "${workspaceFolder}",
      "env": {
        "PROJECTPLANER_MCP_SURFACE": "v2"
      }
    }
  }
}
