{
  "name": "@projectplaner/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "plan": "tsx src/agent-cli.ts",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@projectplaner/core": "workspace:*",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1"
  }
}
