{
  "name": "@projectplaner/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "projectplaner-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/server.node-test.ts",
    "smoke": "tsx src/smoke.ts",
    "smoke:protocol": "tsx src/protocol-smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@projectplaner/core": "workspace:*",
    "@projectplaner/db": "workspace:*",
    "zod": "^3.24.2"
  }
}
