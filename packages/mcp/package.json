{
  "name": "@projectplaner/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "projectplaner-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -w exec vitest run packages/mcp/src/plan.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@projectplaner/core": "workspace:*",
    "@projectplaner/db": "workspace:*",
    "zod": "^3.24.2"
  }
}
